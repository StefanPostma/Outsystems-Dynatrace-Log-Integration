input {
    jdbc {
        jdbc_driver_library => "${JDBC_LIBRARY}"
        jdbc_driver_class => "${JDBC_DRIVER_CLASS}"
        jdbc_connection_string => "${JDBC_CONNECTION_STRING}"
        jdbc_user => "${JDBC_USER}"
        jdbc_password => "${JDBC_PASSWORD}"
        last_run_metadata_path => "${LAST_RUN_DIR}/jdbc.integration.last_run"
        schedule => "${JDBC_SCHEDULE}"

        jdbc_paging_enabled => true
        tracking_column => "instant_unix"
        use_column_value => true
        tracking_column_type => "numeric"

        statement => "
            SELECT
                instant_unix              = DATEDIFF(SECOND,'2021-01-01', instant),
                application_action_name   = i.[Action],
                application_key           = i.[Application_Key],
                application_name          = i.[Application_Name],
                integration_detail_link   = d.[Detail],
                integration_detail_label  = d.[DetailLabel],
                duration                  = i.[Duration],
                application_endpoint_name = i.[Endpoint],
                error_id                  = i.[Error_Id],
                application_espace_id     = i.[Espace_Id],
                application_espace_name   = i.[Espace_Name],
                integration_executor      = i.[Executed_by],
                integration_id            = i.[Id],
                import_latency            = DATEDIFF(minute, i.[Instant],GETDATE()),
                instant                   = i.[Instant],
                integration_expose        = i.[Is_Expose],
                integration_type          = i.[Type],
                message_text              = d.[Message],
                request_key               = i.[Request_Key],
                application_client_ip     = i.[Source],
                tenant_id                 = i.[Tenant_Id]
            FROM [dbo].[oslog_Integration] (nolock) i
                LEFT JOIN [dbo].[oslog_Int_Detail_0] (nolock) d ON i.Id = d.Id
            WHERE DATEDIFF(SECOND,'2021-01-01', instant) > :sql_last_value
        "
    }
}

filter {
    #------------------------------------------------------------------------------#
    # Common part
    #------------------------------------------------------------------------------#
    mutate {
        add_field => {
            "[outsystems][customer][name]" => "${DATA_CUSTOMER_NAME}"
            "[outsystems][location][name]" => "${DATA_LOCATION_NAME}"
            "[deployment][environment]" => "${DATA_ENVIRONMENT_NAME}"
        }

        convert => [ "instant", "string" ] # we need to convert this to a string to be able to parse it into the @timestamp field

        remove_field => [ "instant_unix" ] # this field was only used for controling the query pagination, so we don't need to keep it
    }

    date {
        match => ["instant", "ISO8601"]
        timezone => "Etc/UTC"
    }

    mutate {
        rename => { "instant" => "[log][instant]" } # the value of this field is now in the @timestamp field, so we just keep it as a control value
    }

    #------------------------------------------------------------------------------#
    # Specific part
    #------------------------------------------------------------------------------#
    fingerprint {
        source => ["[integration_id]"]
        target => "[@metadata][fingerprint]"
        method => "MURMUR3"
        concatenate_sources => true
    }

    mutate {
        add_field => { "[outsystems][log][type]" => "Integration" }

        rename => {
            "application_action_name" => "[code][function]"
            "application_key" => "[outsystems][app][key]"
            "application_name" => "[outsystems][app][name]"
            "application_entrypoint_name" => "[outsystems][log][entrypoint_name]"
            "application_endpoint_name" => "[outsystems][log][endpoint]"
            "application_espace_id" => "[outsystems][espace][id]"
            "application_espace_name" => "[outsystems][espace][name]"
            "application_client_ip" => "[http][client_ip]"
            "integration_detail_link" => "[outsystems][log][detail_link]"
            "integration_detail_label" => "[outsystems][log][detail_label]"
            "integration_executor" => "[outsystems][log][executed_by]"
            "integration_id" => "[outsystems][log][uid]"
            "integration_expose" => "[outsystems][integration][type]"
            "integration_type" => "[http][method]"
            "message_text" => "[content]"
            "import_latency" => "[outsystems][logstash][import_latency]"
            "request_key" => "[outsystems][request][key]"
        }
    }

    range {
        ranges => [
            "duration",    0,         200, "field:duration_class:Good",
            "duration", 201, 999999999999, "field:duration_class:Bad"
        ]
    }

    #------------------------------------------------------------------------------#
    # OTEL semantic alignment (see Documentation/OTEL-Field-Mapping.md)
    # Renames of fields that are absent in this pipeline are no-ops.
    #------------------------------------------------------------------------------#
    mutate {
        rename => {
            "duration" => "[outsystems][request][duration]"
            "duration_class" => "[outsystems][logstash][duration_class]"
            "tenant_id" => "[outsystems][tenant][id]"
            "user_id" => "[enduser][id]"
            "user_name" => "[outsystems][user][name]"
            "login_id" => "[outsystems][user][login_id]"
            "server_name" => "[host][name]"
            "client_ip" => "[http][client_ip]"
            "environment_information" => "[outsystems][log][environment_information]"
            "error_id" => "[outsystems][log][error][uid]"
        }
    }
}

output {
    dynatrace {
    	#id => "optional"
    	ingest_endpoint_url => "${DT_ENDPOINT}"
    	api_key => "${DT_APIKEY}"
    	# Only set DT_VERIFYSSLNONE=true for self-signed endpoints (e.g. ActiveGate); leave unset otherwise
    	ssl_verify_none => "${DT_VERIFYSSLNONE:false}"
  	}
}
